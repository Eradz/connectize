Fixes

- Fixed fliker in the chats section when you click on a chat
- the dates grouping in the messages panel is now cleaner, better and more accurate. It's now like that of whatsapp
- the "No Network" toast notification no longer spams the user when there is no network.
- The images attachments no longer gets downloaded/loaded automatically. The user now has to manually download them. Though it still needs so finishing touches (mainly UI/UX) it works well now. The updates needed involves the backend.
- Going to a previous step after you have uploaded an image does not unset the image. Taling about the proile update page.
- If you have already uploaded a profile image before, it would be the default image in the profile update page.
- Changes to profile no longer needs a page refresh to reflect on other parts of the website.

More Fixes

- Clicking on message date categorization header no longer scrolls the whole page
- Cleaned up the manage representatives list section to look more tabular
- Removed room name to allow messages to be marked as read

June 12 2025

- Added the bookmarks tab to the bottom nav on mobile view
- Assigning roles to users now has a toast feedback for when it succeeds and when it doesn't
- Assigning roles to users now works (The invitation now gets sent), also the user can now see representatives that thev've added.
- Increased the interval for many polling including companies, posts, services, products and more to reduce bandwidth. Some of these requests do not even need polling in the first place but a long interval is still better than a short one.
- the interval of the poll Request to get all users in the platform has also been increased.
